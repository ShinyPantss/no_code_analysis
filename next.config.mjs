/** @type {import('next').NextConfig} */
export const images = {
    remotePatterns: [
        {
            protocol: 'https',
            hostname: 'wxaczqrilsebdiqdcyle.supabase.co',
            port: '',
            pathname: '/**',
        },
    ],
};